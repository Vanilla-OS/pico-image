name: Vanilla Pico
id: pico
vibversion: 1.0.3

stages:
- id: build
  base: scratch
  singlelayer: true
  labels:
    maintainer: Vanilla OS Contributors
  adds:
    - srcdst:
        rootfs/vanilla-pico.tar.zst: /
  cmd:
    exec:
      - /bin/bash
